apiVersion: v1
kind: Service
metadata:
  name: {{ include "renci-update.fullname" . }}
  labels:
    {{- include "renci-update.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "renci-update.selectorLabels" . | nindent 4 }}